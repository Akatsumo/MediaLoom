user:
  name: sumit-yadav
  github: akatsumo

runtime:
  image: "python:3.11-slim"

commands:
  build:
    - "pip install -r requirements.txt"
  package: ""
  start: "python m Restriction"

repository:
  branch: "main"
  auto_deploy: true
